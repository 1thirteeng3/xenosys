# =============================================================================
# XenoSys Docker Compose Configuration (V1.0.0-RC-PROD)
# =============================================================================

services:
  # -------------------------------------------------------------------------
  # 1. Gateway Service (Node.js)
  # -------------------------------------------------------------------------
  gateway:
    build:
      context: ./nexus/gateway
    container_name: xenosys-gateway
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - REDIS_HOST=redis
    depends_on:
      - core
      - redis
    restart: unless-stopped
    networks:
      - xenosys-network

  # -------------------------------------------------------------------------
  # 2. Core Service (Python)
  # -------------------------------------------------------------------------
  core:
    build:
      context: ./nexus/core
    container_name: xenosys-core
    ports:
      - "50051:50051"
    environment:
      - PYTHONUNBUFFERED=1
      - GRPC_HOST=0.0.0.0
      - REDIS_HOST=redis
      # Conexões com as memórias corrigidas
      - CORTEX_ENDPOINT=http://cortex:8000
      - OPENVIKING_ENDPOINT=http://openviking:8080
      - OBSIDIAN_MCP_URL=http://mcp-obsidian:3001/sse
      - MEMBASE_MCP_URL=http://mcp-memory:9000/sse
    depends_on:
      - redis
    restart: unless-stopped
    networks:
      - xenosys-network

  # -------------------------------------------------------------------------
  # 3. Redis (Message Broker & Cache)
  # -------------------------------------------------------------------------
  redis:
    image: redis:7-alpine
    container_name: xenosys-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis-data:/data
    restart: unless-stopped
    networks:
      - xenosys-network

  # -------------------------------------------------------------------------
  # 4. OpenViking (Memória Episódica da Volcengine)
  # -------------------------------------------------------------------------
  openviking:
    build: https://github.com/volcengine/OpenViking.git#main
    container_name: xenosys-openviking
    ports:
      - "8080:8080"
    volumes:
      - openviking-data:/data
    restart: unless-stopped
    networks:
      - xenosys-network

  # -------------------------------------------------------------------------
  # 5. Cortex (Memória Semântica da Abbacus Group)
  # -------------------------------------------------------------------------
  cortex:
    build: https://github.com/abbacusgroup/cortex.git#main
    container_name: xenosys-cortex
    ports:
      - "8000:8000"
    volumes:
      - cortex-data:/data
    restart: unless-stopped
    networks:
      - xenosys-network

  # -------------------------------------------------------------------------
  # 6. BrainSys (Graph-based Contextual Memory / L4)
  # -------------------------------------------------------------------------
  mcp-memory:
    image: node:20-alpine
    container_name: xenosys-mcp-memory
    ports:
      - "9000:9000"
    # IMPORTANTE: Para o XenoSys Core ler via rede (SSE), usamos npx em modo servidor web
    command: npx -y @modelcontextprotocol/server-memory
    volumes:
      - xenosys-l4-data:/app/memory_data
    restart: unless-stopped
    networks:
      - xenosys-network

  # -------------------------------------------------------------------------
  # 7. MCP Server - Obsidian (Long-term Memory / Notes)
  # -------------------------------------------------------------------------
  mcp-obsidian:
    image: node:20-alpine
    container_name: xenosys-mcp-obsidian
    ports:
      - "3001:3001"
    command: npx -y @modelcontextprotocol/server-obsidian --dir /vault
    volumes:
      - obsidian-vault:/vault
    restart: unless-stopped
    networks:
      - xenosys-network

# =============================================================================
# Networks
# =============================================================================
networks:
  xenosys-network:
    driver: bridge
    name: xenosys-network

# =============================================================================
# Volumes
# =============================================================================
volumes:
  redis-data:
    name: xenosys-redis-data
  cortex-data:
    name: xenosys-cortex-data
  openviking-data:
    name: xenosys-openviking-data
  obsidian-vault:
    name: xenosys-obsidian-vault
  xenosys-l4-data:
    name: xenosys-l4-data
